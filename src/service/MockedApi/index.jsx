const API = (userId, request=null) => {
  console.log(`../../data/user/${userId}/${request}`)
  return (
    <>
      <>
        `../../data/user/${userId}/${request}`
      </>
    </>
  )
}

export default API;