const API = (userId, request=null) => {
  console.log(`http://localhost:3000/user/${userId}/${request}`)
  return (
    <>
      <>
        `http://localhost:3000/user/${userId}/${request}`
      </>
    </>
  )
}

export default API;